ext fn puts(string) -> i32

fn 関数1(a: i32) -> i64{
  if a > 0 {
    3
  } else if true {
    if true {
      if false { ret 30 }
    }
    4
  } else {
    99
  }
}

fn b() ->i32{
  0
}
fn o() {
  0
}

fn main()->i32 {
  var a = 'a';
  let c = b() + '4';
  let d = 関数1(a)
  let vv =  3
  if c >= 10 {
    let str = "あいう"
    puts(str)
  }
  ret c
}
