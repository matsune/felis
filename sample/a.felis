ext fn puts(string) -> i32

fn 関数1(a: i32) -> i32{
  if a < 0 {
    ret 3
  } else if true {
    if true {
      if false { ret 30 }
    }
    ret 4

  } else {
    ret 99
  }
}

fn main()->i32 {
  let a = 20
  let c = 関数1(a)
  if c >= 10 {
    let str = "あいう"
    puts(str)
  }
  ret c
}
