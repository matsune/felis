ext fn puts(string) -> i32

fn 関数1(a: i64) -> i32{
  if a < 0 {
    ret 3
  } else if true {
    if true {
      if false { ret 30 }
    }
    ret 4

  } else {
    ret 99
  }
}

fn b() ->i32{}
fn o() {}

fn main()->i32 {
  var a = 'a';
  let c = b() + '4';
  let d = 関数1(a)
  let vv =  3
  if c >= 10 {
    let str = "あいう"
    puts(str)
  }
  ret c
}
